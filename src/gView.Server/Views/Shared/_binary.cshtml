@{
    Layout = null;

    var response = this.Context.Response;

    if (ViewData["data"] != null)
    {
        response.ContentType = (string)ViewData["content-type"];
        var buffer = (byte[])ViewData["data"];
        await response.Body.WriteAsync(buffer, 0, buffer.Length);
    }
}
