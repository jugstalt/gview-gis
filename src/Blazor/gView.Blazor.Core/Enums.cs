using System;

namespace gView.Blazor.Core
{
    public enum MessageBoxButtons
    {
        OkCancel,
        YesNoCancel,
    }

    public enum DialogResult
    {
        OK,
        Cancel,
        Yes,
        No
    }
}
